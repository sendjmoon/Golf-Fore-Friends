'use strict';

module.exports = function(app) {
  app.controller('DashboardController', ['$rootScope', 'GameService', 'UserService', 'FriendService', function($rs, GameService, UserService, FriendService) {
    
  }]);
};
