'use strict';

module.exports = function(app) {
  app.controller('NewGameController', ['$rootScope', function($rs) {

    let ctrl = this;
    ctrl.players = [];

    ctrl.init = function() {
      
    }
  }]);
}
