'use strict';

module.exports = function(app) {
  app.controller('LeaderboardController', ['$rootScope', '$scope', function($rs, $scope) {
    const ctrl = this;

    ctrl.fillPlayerBar = function() {
      const $barFill = $('.gff-progress-bar-fill');

    }
    
  }]);
}
